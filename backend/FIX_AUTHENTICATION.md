# Решение проблемы аутентификации (P1000)

## Проблема:
```
Error: P1000: Authentication failed against database server
```

## Причины и решения:

### 1. ❌ Неправильный пароль

**Решение:**
- Проверьте пароль в Supabase Dashboard
- Settings → Database → Database password
- Если забыли пароль, сбросьте его в настройках

### 2. ❌ Специальные символы в пароле не экранированы

Если пароль содержит специальные символы, их нужно закодировать в URL-формат:

| Символ | Код |
|--------|-----|
| @ | %40 |
| # | %23 |
| $ | %24 |
| % | %25 |
| & | %26 |
| + | %2B |
| = | %3D |
| / | %2F |
| ? | %3F |
| : | %3A |
| ; | %3B |
| [ | %5B |
| ] | %5D |
| { | %7B |
| } | %7D |
| | | %7C |
| \ | %5C |
| ^ | %5E |
| ~ | %7E |
| ` | %60 |
| < | %3C |
| > | %3E |
| " | %22 |
| ' | %27 |
| пробел | %20 |

**Пример:**
- Пароль: `MyP@ss#123`
- В URL: `MyP%40ss%23123`

### 3. ❌ Неправильный формат DATABASE_URL

**Правильный формат:**
```
postgresql://postgres:[ПАРОЛЬ]@[ХОСТ]:5432/postgres?sslmode=require
```

**Где взять данные:**
1. Откройте: https://supabase.com/dashboard/project/bebwdgocsfydqqtrcwvp/settings/database
2. Найдите секцию "Connection string"
3. Выберите "Direct connection" (НЕ "Connection pooling")
4. Скопируйте строку подключения
5. Замените `[YOUR-PASSWORD]` на ваш пароль (с экранированием, если нужно)

### 4. ✅ Пошаговая инструкция:

#### Шаг 1: Получите правильный connection string

1. Откройте Supabase Dashboard
2. Settings → Database
3. Найдите "Connection string"
4. Выберите "Direct connection" (порт 5432)
5. Скопируйте строку

#### Шаг 2: Создайте файл `.env` в папке `backend/`

```env
DATABASE_URL="postgresql://postgres:ВАШ_ПАРОЛЬ@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require"
```

**Важно:**
- Используйте кавычки вокруг URL
- Если пароль содержит специальные символы, закодируйте их
- Используйте порт **5432** (direct connection), не 6543

#### Шаг 3: Проверьте подключение

```bash
cd backend
npx prisma db push
```

### 5. 🔧 Альтернатива: Использовать Supabase CLI

Если проблемы продолжаются, используйте Supabase CLI для миграций:

```bash
# Линк проекта
supabase link --project-ref bebwdgocsfydqqtrcwvp

# Отправить миграцию
supabase db push
```

### 6. 🧪 Проверка подключения вручную

Проверьте подключение через psql (если установлен):

```bash
psql "postgresql://postgres:ВАШ_ПАРОЛЬ@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require"
```

Если подключение работает, значит проблема в формате URL в .env файле.

### 7. ⚠️ Частые ошибки:

1. **Использование pooler (порт 6543)** - не работает для миграций
2. **Отсутствие кавычек** в .env файле
3. **Неправильное экранирование** специальных символов
4. **Пробелы** в пароле не закодированы (%20)
5. **Старый пароль** - пароль мог быть изменен

### 8. ✅ Правильный пример .env:

```env
# Простой пароль без специальных символов
DATABASE_URL="postgresql://postgres:simplepassword123@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require"

# Пароль со специальными символами (MyP@ss#123)
DATABASE_URL="postgresql://postgres:MyP%40ss%23123@aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require"
```

