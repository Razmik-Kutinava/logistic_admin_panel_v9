# 🚚 Logistics Admin Panel v9

Административная панель для управления логистической системой с функционалом управления водителями, устройствами, зонами и пользователями.

## 📋 Описание проекта

Проект представляет собой полнофункциональную админ-панель для логистической компании, состоящую из:

- **Backend** - REST API на базе NestJS с использованием Prisma ORM и Supabase
- **Frontend** - Современный веб-интерфейс на SolidJS с Vite

### Основные модули:

- 👤 **Водители (Drivers)** - Управление информацией о водителях, их лицензиях и статусах
- 📱 **Устройства (Devices)** - Управление GPS-трекерами и другими устройствами, привязка к водителям
- 📍 **Зоны (Zones)** - Управление зонами доставки и их привязка к водителям
- 👥 **Пользователи (Users)** - Управление пользователями системы

## 🛠 Технологический стек

### Backend
- **NestJS** - Прогрессивный Node.js фреймворк
- **Prisma** - ORM для работы с базой данных
- **PostgreSQL** - Реляционная база данных (через Supabase)
- **TypeScript** - Типизированный JavaScript
- **Supabase** - Backend-as-a-Service платформа

### Frontend
- **SolidJS** - Реактивный UI фреймворк
- **Vite** - Быстрый сборщик и dev-сервер
- **TypeScript** - Типизированный JavaScript

## 📦 Установка и настройка

### Предварительные требования

- Node.js (версия 18 или выше)
- npm или pnpm
- PostgreSQL база данных (или аккаунт Supabase)
- Git

### Установка зависимостей

1. Клонируйте репозиторий:
```bash
git clone https://github.com/Razmik-Kutinava/logistic_admin_panel_v9.git
cd logistic_admin_panel_v9
```

2. Установите зависимости для backend:
```bash
cd backend
npm install
```

3. Установите зависимости для frontend:
```bash
cd ../frontend
npm install
```

### Настройка базы данных

1. Создайте файл `.env` в папке `backend` на основе примера:
```bash
cd backend
```

2. Добавьте следующие переменные окружения в `.env`:
```env
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"
SUPABASE_URL="your_supabase_url"
SUPABASE_KEY="your_supabase_key"
```

3. Выполните миграции Prisma:
```bash
npm run prisma:generate
npm run prisma:push
```

### Настройка Frontend

1. Создайте файл `.env` в папке `frontend` (если требуется):
```bash
cd frontend
```

2. Убедитесь, что переменная `VITE_API_URL` указывает на адрес вашего backend API (по умолчанию `http://localhost:3000`)

## 🚀 Запуск проекта

### Запуск Backend

```bash
cd backend
npm run start:dev
```

Backend будет доступен по адресу: `http://localhost:3000`

### Запуск Frontend

```bash
cd frontend
npm run dev
```

Frontend будет доступен по адресу: `http://localhost:5173` (или другой порт, указанный Vite)

## 📁 Структура проекта

```
logistic_admin_panel_v9/
├── backend/                 # NestJS Backend приложение
│   ├── src/
│   │   ├── devices/        # Модуль управления устройствами
│   │   ├── driver/         # Модуль управления водителями
│   │   ├── zones/          # Модуль управления зонами
│   │   ├── users/          # Модуль управления пользователями
│   │   ├── prisma/         # Prisma сервис и модуль
│   │   └── main.ts         # Точка входа приложения
│   ├── prisma/
│   │   └── schema.prisma   # Схема базы данных
│   └── package.json
├── frontend/               # SolidJS Frontend приложение
│   ├── src/
│   │   ├── components/     # React компоненты
│   │   │   ├── DriversTab.tsx
│   │   │   ├── DevicesTab.tsx
│   │   │   ├── ZonesTab.tsx
│   │   │   └── UsersTab.tsx
│   │   ├── services/       # API сервисы
│   │   └── App.tsx         # Главный компонент
│   └── package.json
└── README.md               # Этот файл
```

## 🔧 Доступные команды

### Backend

```bash
# Разработка
npm run start:dev          # Запуск в режиме разработки с hot-reload

# Сборка
npm run build              # Сборка проекта для production

# Production
npm run start:prod         # Запуск production версии

# Prisma
npm run prisma:generate    # Генерация Prisma клиента
npm run prisma:push        # Применение изменений схемы к БД

# Тестирование
npm run test               # Запуск unit тестов
npm run test:e2e           # Запуск e2e тестов
npm run test:cov           # Покрытие кода тестами

# Линтинг и форматирование
npm run lint               # Проверка кода линтером
npm run format             # Форматирование кода
```

### Frontend

```bash
# Разработка
npm run dev                # Запуск dev-сервера
npm start                  # Алиас для npm run dev

# Сборка
npm run build              # Сборка для production

# Preview
npm run serve              # Предпросмотр production сборки
```

## 📊 API Endpoints

### Водители (Drivers)
- `GET /driver` - Получить список всех водителей
- `GET /driver/:id` - Получить водителя по ID
- `POST /driver` - Создать нового водителя
- `PATCH /driver/:id` - Обновить водителя
- `DELETE /driver/:id` - Удалить водителя

### Устройства (Devices)
- `GET /devices` - Получить список всех устройств
- `GET /devices/:id` - Получить устройство по ID
- `POST /devices` - Создать новое устройство
- `PATCH /devices/:id` - Обновить устройство
- `DELETE /devices/:id` - Удалить устройство

### Зоны (Zones)
- `GET /zones` - Получить список всех зон
- `GET /zones/:id` - Получить зону по ID
- `POST /zones` - Создать новую зону
- `PATCH /zones/:id` - Обновить зону
- `DELETE /zones/:id` - Удалить зону

### Пользователи (Users)
- `GET /users` - Получить список всех пользователей
- `GET /users/:id` - Получить пользователя по ID
- `POST /users` - Создать нового пользователя
- `PATCH /users/:id` - Обновить пользователя
- `DELETE /users/:id` - Удалить пользователя

## 🗄 Модели данных

### User
- `id` - Уникальный идентификатор
- `email` - Email адрес (уникальный)
- `name` - Имя пользователя
- `createdAt` - Дата создания
- `updatedAt` - Дата обновления

### Driver
- `id` - Уникальный идентификатор
- `name` - Имя водителя
- `phone` - Телефон (уникальный)
- `email` - Email (уникальный, опционально)
- `licenseNumber` - Номер лицензии (уникальный)
- `status` - Статус (по умолчанию "active")
- `devices` - Связанные устройства
- `zones` - Связанные зоны
- `createdAt` - Дата создания
- `updatedAt` - Дата обновления

### Device
- `id` - Уникальный идентификатор
- `name` - Название устройства
- `imei` - IMEI номер (уникальный)
- `model` - Модель устройства (опционально)
- `status` - Статус (по умолчанию "active")
- `driverId` - ID водителя (опционально)
- `driver` - Связанный водитель
- `createdAt` - Дата создания
- `updatedAt` - Дата обновления

### Zone
- `id` - Уникальный идентификатор
- `name` - Название зоны
- `address` - Адрес
- `city` - Город
- `zipCode` - Почтовый индекс (опционально)
- `status` - Статус (по умолчанию "active")
- `driverId` - ID водителя (опционально)
- `driver` - Связанный водитель
- `createdAt` - Дата создания
- `updatedAt` - Дата обновления

## 🔒 Безопасность

- Все переменные окружения должны храниться в `.env` файлах
- Не коммитьте `.env` файлы в репозиторий
- Используйте сильные пароли для базы данных
- Настройте CORS для production окружения

## 🐛 Решение проблем

### Проблемы с Prisma на ARM64 Windows

Если возникают проблемы с Prisma на ARM64 архитектуре Windows, см. файл `backend/FIX_ARM64_WINDOWS.md`

### Проблемы с подключением к базе данных

Проверьте файл `backend/ENV_SETUP.md` для настройки переменных окружения.

### Проблемы с Prisma Engine

См. файл `backend/FIX_PRISMA_ENGINE.md` для решения проблем с Prisma Engine.

## 📝 Лицензия

Этот проект является приватным и не имеет открытой лицензии.

## 👥 Авторы

- Razmik Kutinava

## 🔗 Ссылки

- [NestJS Documentation](https://docs.nestjs.com)
- [SolidJS Documentation](https://www.solidjs.com)
- [Prisma Documentation](https://www.prisma.io/docs)
- [Supabase Documentation](https://supabase.com/docs)

## 📞 Поддержка

При возникновении проблем создайте issue в репозитории проекта.

---

**Версия:** 9.0  
**Последнее обновление:** 2024

